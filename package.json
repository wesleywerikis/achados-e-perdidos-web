{
  "name": "achados-e-perdidos-web",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "dev:vercel": "npx vercel dev",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d",
    "dev:full": "docker compose up -d && node scripts/dev-server.js"
  },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "dotenv": "^16.4.7",
    "vercel": "^41.0.0"
  }
}