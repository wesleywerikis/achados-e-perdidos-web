# Banco local via Docker (padrao do docker-compose.yml)
POSTGRES_URL=postgresql://achados:achados@localhost:5432/achados_perdidos

# Se usar banco na nuvem (Vercel/Neon), substitua pela URL do painel:
# POSTGRES_URL=postgresql://usuario:senha@host:5432/banco
